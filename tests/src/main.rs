//! Entry point for benchmark tests.
//! This won't work until we hone in DSL and requirements
//! and have a baseline solver implementation.

//mod egg_baseline;

use epo_proc_macros::run_suite;

//use crate::egg_baseline::EggSolver;

fn main() {
    //run_suite!(EggSolver);
}
